## 残す

- 対象：`index.html` の Hero／`.compare-visual`  
  現在：図面・証明書・判断待ち件数を表示。変更理由：Evidence Deskの方向性を維持し、高さと余白だけ調整する。

- 対象：`index.html` の `.feature-grid`／`preview.js`  
  現在：実画面サムネイル付きの4機能プレビュー。変更理由：短いトップで機能を把握できる構成として活用する。

- 対象：`app.js` の `screens`／`images/`  
  現在：4つの実画面定義と画像が存在。変更理由：既存実画面は削除せず、表示先を整理して再利用する。

## 移す

- 対象：`story-mode.html` の既存8ステップ  
  現在：HeroのメインCTAから遷移する本編。変更理由：「詳しい仕組みを見る」の補助コンテンツへ移し、8ステップは保持する。

- 対象：`index.html` の `.screen-gallery`／`.screen-detail`  
  現在：トップ下部に実画面一覧と詳細領域を配置。変更理由：専用ビューへ移し、同一画面内の4タブで閲覧できるようにする。

## 削る

- 対象：`index.html` の `.story-link`  
  現在：Story Modeの説明が独立セクションになっている。変更理由：補助リンクへ集約し、トップの縦長化を解消する。

- 対象：`index.html` の大きな実画面一覧枠・重複説明／`styles.css` の上下余白  
  現在：プレビューの下に画像一覧と説明が続く。変更理由：トップをHero・4機能プレビュー・CTAの「1画面＋少し」に収める。

- 対象：`app.js`／`flow.js` の `scrollIntoView()`  
  現在：詳細表示やデモ開始時に自動スクロールする実装。変更理由：同一画面内の表示切り替えへ変更する。

## 変更する

- 対象：`index.html` の `.hero-actions`  
  現在：メインCTAは8ステップ、実画面CTAはページ下へ遷移。変更理由：横長コンソールと4タブの実画面ビューへ接続する。

- 対象：`flow.js`／`index.html` のスクリプト接続  
  現在：2段階自動再生の実装は未読込で、開始対象の `open-product` 要素も存在しない。変更理由：既存実装を整理し、メイン体験として動作させる。

- 対象：`flow.js` の `.flow-console`／`styles.css`  
  現在：簡易表示と説明文が中心で、現在の本編はPCでも最大幅430px。変更理由：1440pxで図面・証明書・判定・人判断・承認を横方向に配置する。

- 対象：`flow.js` の `startFlow()`  
  現在：照合と保留の状態を順送りする構成。変更理由：操作1「照合を開始」で図面表示→証明書表示→判定→要確認2件まで自動再生する。

- 対象：`flow.js` の `continueFlow()`  
  現在：保留・承認・基準改定の状態を順送りする構成。変更理由：操作2「確認を進める」で人判断→根拠記録→承認→基準改定まで自動再生する。

- 対象：`app.js` の画面切り替え／`index.html` の実画面表示領域  
  現在：機能カードからページ下部の詳細を開く。変更理由：「照合／保留一覧／承認／基準改定」の4タブで同じ領域を切り替える。

- 対象：`flow.js` の状態表示・操作ボタン  
  現在：説明文の更新と追加ナビゲーションが中心。変更理由：項目の強調・件数・根拠・承認状態の変化で伝え、メイン操作を2回に固定する。
