監査結果を確認しました。

今回は、監査結果のうち以下だけを実装してください。
全面再設計は禁止です。

## 実装する

### 1. トップページ
- Heroは現状のEvidence Desk方向を維持
- `.feature-grid` の4機能プレビューを維持
- `.screen-gallery` / `.screen-detail` はトップから外す
- `.story-link` の独立セクションは削除
- トップは Hero + 4機能プレビュー + CTA の「1画面＋少し」に収める
- 自動スクロール `scrollIntoView()` は削除する

### 2. CTAの役割を明確化
Heroのボタンは以下に固定する。

- 「デモを開く」
  → 新しい横長コンソール形式のメイン体験を開始

- 「実画面を見る」
  → 照合 / 保留一覧 / 承認 / 基準改定 の4タブビューを開く

ページ下へスクロールする挙動は禁止。

### 3. メインデモ
`flow.js` を使って、PC向け横長コンソールへ変更する。

1440px desktopでは、
中央430px程度の縦長レイアウトを使用しない。

表示領域は横長で、
- 図面
- 証明書
- 判定
- 人判断
- 承認
が視覚的につながるようにする。

### 4. 操作回数
メイン操作は2回に固定。

操作1：
「照合を開始」
→ 図面表示
→ 証明書表示
→ 判定
→ 要確認2件
まで自動で進む

操作2：
「確認を進める」
→ 人判断
→ 根拠記録
→ 承認
→ 基準改定
まで自動で進む

1クリック1線、1クリック1ラベルのような細かい操作は禁止。

### 5. 実画面ビュー
既存の4実画面と画像は削除しない。

以下の4タブで同じ表示領域を切り替える。

- 照合
- 保留一覧
- 承認
- 基準改定

既存 `app.js` の実画面定義と `images/` を再利用する。

### 6. 既存8ステップ
既存8ステップは削除しない。

ただしメイン体験には使わず、
「詳しい仕組みを見る」という補助リンクから開く。

現時点では8ステップ自体のUI修正は行わない。

---

## 今回触らないもの

- 既存8ステップの中身
- 実画面画像4種
- Heroの基本デザイン
- Heroの主要コピー
- 実データ / サンプル値
- 新しい機能追加
- 新しい説明セクション追加

---

## 実装順序

Phase 1
トップの短縮とCTA導線だけ修正

Phase 2
4タブ実画面ビューを実装

Phase 3
横長コンソールを実装

Phase 4
2操作の自動進行を実装

Phase 5
UI polish / motion

各Phase終了時に、次へ進む前に
1440px desktopのスクリーンショットを確認してください。

## Phase 1完了後のスクリーンショット確認

1440px desktopのスクリーンショットで確認する項目は、以下の4点だけとする。

- トップが長くなくなったか
- Heroから「デモ」と「実画面」の行き先が明確か
- 4機能プレビューが邪魔せず機能紹介になっているか
- 下方向に意味なくスクロールさせられないか

## 最重要

各Phaseで勝手に次のPhaseへ進まないでください。
Phase 1完了時点で一度停止し、変更内容とスクリーンショット確認結果を報告してください。
