# 受入検査 承認ワークフローデモ 要件定義 v0.1

仮称: **承認デモ**（指定と現物の突き合わせ、そのあとの承認の連なり）

第1弾「原因特定デモ」の姉妹デモ。あちらが**後工程で見つかる不具合**を扱うのに対し、
本デモは**入口で止められる不一致**を扱う。

---

## 1. このデモの位置づけ

### 1.1 目的

受入検査から出荷検査までの承認の連なりを題材に、
「中身を見ているのは最初の一回だけ」という構造を可視化する。

営業が説明しながら見せる用途と、名刺QRから単独で開かれる用途の両方を想定する。

### 1.2 体験の種類

自動進行型。閲覧者が触るのは「次へ」のみ。第1弾と同じ。

### 1.3 このデモが主張すること

> 書類は全部そろっている。整合もしている。それでも違う。

不正や手抜きの話にはしない（§2.4）。誰も間違っていないのにすり抜ける、という構造だけを扱う。

---

## 2. 題材設定

### 2.1 工程

受入検査 → 加工 → 中間検査 → 組立 → 出荷検査

各工程に承認がある。**前工程で承認されていることを確認し、その項目は信じて自工程を進める。**
これが「後工程は書類の整合性しか見ていない」状態の実体であり、本デモの土台。

### 2.2 突き合わせるもの

| | 内容 |
|---|---|
| 基準側 | 図面で決められた指定（材質、表面処理、寸法規格など） |
| 現物側 | 届いた材料の材料証明書に書かれている記載 |

### 2.3 実務で起きること

- 材料証明書に、図面指定材料であることが記載されていない
- 図面の表記と証明書の表記が異なる（同じものを指している場合もある）
- ロット番号が不足していて追跡できない

いずれも**書類は添付されており、承認も下りている**状態で起きる。

### 2.4 扱わないこと（重要）

**改ざん・偽装・検査員の不正は題材にしない。**

技術的にやることは同じだが、扱いを誤ると「あなたの検査員は信用できない」という提案になり、
品質保証部門ほど強く身構える。デモ本体は表記の揺れとロット番号の不足に限定する。

背景として語る価値はあるため、商談での口頭補足に留める。

---

## 3. 照合の3状態

本デモの中核。図形・承認結果・AIの役割が、この3状態で一本に通る。

| 状態 | 意味 | 図形表現（§7） | 承認の結果 | 誰が判断するか |
|---|---|---|---|---|
| 一致 | 指定と記載が対応している | 縦の線がまっすぐ | 承認 | 自動照合で確認済み |
| 要確認 | 表記が異なる／対応が読み取れない | 縦の線が斜めになる | 差戻し または 保留 | **人が判断** |
| 不足 | 記載そのものがない | 下側の受けがなく、線が宙で終わる | 保留 | **人が判断** |

### 3.1 「不一致」と断定しない理由

表記が違っても同じ材質を指す場合がある（旧記号、規格の別名、メーカー独自表記）。
システムが「違う」と断定すると誤検知になり、現場の信頼を即座に失う。

**システムは「揃っている／要確認／記載なし」を提示するところまで。判断は人が行う。**

この分担は第1弾（候補を絞るのはシステム、原因の確定は人）と同一。
2本を並べたときに一貫した思想として読めることを重視する。

---

## 4. 設計原則

第1弾から継承。★は本デモで追加。

### 原則1 — ラベルを消しても意味が残ること

**検算**: 全テキストを非表示にして以下が読めるか。読めなければ作り直す。

- 上下2列が対応していること
- そのうち1組だけ対応が崩れていること
- 最初の承認だけが上を向き、以降は左を向いていること

### 原則2 — 表示された要素は消さない

崩れた対応も、通過した承認も、画面に残り続ける。

### 原則3 — 同時に2箇所を動かさない

### 原則4 — 等間隔で動かさない

### ★原則5 — 比喩を作らない

「鎖」「バトン」「フィルター」などの比喩は使わない。
描くのは、実際に何を見ているかという対象の向きだけ。

### ★原則6 — 承認フローの形を描かない

誰から誰へ回るか、何段階あるかは会社ごとに違う。
そこを描いた瞬間に「うちとは違う」で読まれなくなる。

**描くのは承認の縦方向（何がぶら下がっているか）であって、横方向（誰に回るか）ではない。**
工程名は位置を示すラベルとしてのみ置き、承認の順路としては描かない。

---

## 5. 画面構成

縦積み。iPhone縦持ち（横幅370px基準）。

```
┌──────────────────────────────┐
│  図面（線画・寸法線あり）              │  ← 基準
│         │                            │
│  図面の指定      ■ ■ ■ ■           │  ← 上段
│                  │ │ │ │            │  ← 対応の線
│  材料証明書の記載 ■ ■ ／ ■          │  ← 下段（1組が斜め）
│                                      │
│  ─●───●──●──●──●──   │  ← 承認の連なり
│    受入  加工 中間 組立 出荷          │
└──────────────────────────────┘
```

### 5.1 図面

写真ではなく**線画**とする。寸法線・引出線があれば図面と読める。
写真は業種を限定し、線画は限定しない。

### 5.2 承認の連なり

- 受入検査の丸からは**上向き**の線が伸び、突き合わせを見ている
- 加工・中間検査・組立・出荷検査の丸からは**左向き**の線が伸び、直前の承認を見ている
- 向きの差だけで「中身を見たのは一回」が読めること

丸は5つとも同じ形・同じ大きさにする。**承認は中身によらず同じ見た目で残る**ことの表現であり、
受入だけを強調しない（ラベルの色だけで区別する）。

---

## 6. 進行仕様

全9段。「次へ」のみ。自動遷移しない。

| 段 | 内容 | 意図 |
|---|---|---|
| S0 | 図面と、そこから降りてくる指定の並び | 基準があることを先に置く |
| S1 | 材料証明書の記載が下段に並ぶ | 突き合わせる2つが揃う |
| S2 | 対応の線が**1本ずつ順に**引かれる | 人が目視で照合している時間 |
| S3 | 承認の連なりが現れる。受入は上向き、以降は左向き | 構造の提示 |
| S4 | 崩れている1組の色がゆっくり変わる | 見えていたのに気づかなかった、を体験させる |
| S5 | 後工程の承認からは、その崩れに線が届かない | 後工程では見つからない |
| S6 | 対応の線が**一斉に**引き直される（自動照合） | S2との速度差が価値そのもの |
| S7 | 崩れた1組だけが人に上がる。他は通過 | システムは絞る、人が判断する |
| S8 | 図面側の指定に項目が1つ足される。そこにも承認が付く | 基準の更新もまた承認を通る |

### 6.1 S2とS6の対比が本デモの中心

- **S2** は1本ずつ、間隔を不揃いにして引く（合計3〜4秒）
- **S6** は全本を0.3秒以内に引き終える

同じ絵に対する所要時間の差だけで、自動化の価値が出る。数字を出す必要はない。

### 6.2 S8で省略してはいけないこと

**基準の改定が自動で反映される絵にしない。**

規定・検査基準の改定は版管理と教育を伴う重い手続きであり、
そこを軽く描くと品質保証の実務者に即座に見抜かれる。
改定そのものにも承認が付くことを、必ず1拍として描く。

---

## 7. 照合項目の図形仕様

「ただの四角」を意味のある形にするための中核仕様。

### 7.1 上段・下段

上段（図面の指定）と下段（材料証明書の記載）に、同数の受けを横並びに置く。

### 7.2 対応の表現

上段と下段の同じ項目を、**縦の細い線でつなぐ**。

| 状態 | 表現 |
|---|---|
| 一致 | 線が**垂直**。上下の受けの左端が揃っている |
| 要確認 | 下段の受けが**わずかに横にずれ**、線が**斜め**になる |
| 不足 | 下段に受けがなく、線が**途中で終わる**（破線） |

**「ずれ」を、そのまま位置のずれとして描く。** 比喩を挟まないので説明が要らない。
斜めの線は1本だけにする。複数あると「雑な会社」に見え、主題がぼける。

### 7.3 やらないこと

- 受けの中にアイコンを置く
- 一致に○、不一致に×を付ける（記号に頼ると原則1の検算に落ちる）
- 色だけで状態を区別する（形で区別し、色は強調にのみ使う）

### 7.4 ラベル

各受けの下に、9〜10pxで項目名を置く。候補は次の4つ。

```
材質記号 ／ 表面処理 ／ 寸法規格 ／ ロット番号
```

ずれるのは **材質記号**、不足は **ロット番号** を想定する。
どちらか一方に絞るかは §13-C。

---

## 8. 第2部（記録の中身）

第1弾と同じく、本編の最終段でボタンから開く。実画面のスクリーンショットを縦に並べる。

| # | 画面 | 色チップ | 添えるテキスト |
|---|---|---|---|
| 1 | 受入検査の照合画面（図面指定と材料証明書を並べ、3状態を表示） | 無彩色 | 何を突き合わせているか |
| 2 | 要確認として人に上がった状態（保留） | 警戒色 | 表記が違う具体例、ロット番号の桁不足 |
| 3 | 承認画面（照合結果が承認に紐づいている） | 確定色 | 承認に何が残るか |

### 8.1 画像の扱い

- 実案件の画面は使わない。同一UI系統で作成した画面イメージとする
- 3枚とも同じシステムの別画面に見えること
- **読ませたい中身は画像に埋めず、画像の下にテキストで置く**（第1弾で確定した方針）
- 既定では幅いっぱいに収め、「拡大して見る」で原寸相当＋要所へ自動スクロール

---

## 9. 着地画面

名刺QRからの単独閲覧を想定し、本編の前に1画面置く（第1弾と同構成）。

- 社名 / 一行の見出し / 実画面1枚 / 3行の要約 / 「仕組みを見る」 / 所要時間
- **見出しに製造業の専門語を入れない**。「指定したものが、本当にそれか」の水準にする
- 画像は着地用の1枚のみ先読みし、第2部の画像は最終段で遅延読み込みする

---

## 10. 文言仕様

### 10.1 キャプション

各段に2行。太字1行（状態）＋補足1行（意味）。

第1弾は1行だったが、本デモは前提知識を要するため補足行を常設する。
補足行は**製造業に携わっていない人向け**と位置づける。

### 10.2 禁止事項

- 造語、業界で使われていない言い回し
- 定着していない比喩
- そのまま声に出せない日本語

### 10.3 図の下の常時表示テキスト

3行以内。図が何を描いているかの説明を置く。分かる人は読み飛ばせる位置とする。

---

## 11. 技術要件

第1弾に準拠。

- 単一HTML、外部依存なし、画像はbase64埋め込み
- 横幅370px基準／760px以上でPCレイアウトに切替
- フォントは Noto Sans CJK JP（外部読み込みなしのフォントスタック）
- **iOSダークモード対策は3層 + 要素ごとの実値強制**
  - `<meta name="color-scheme" content="light">`
  - `color-scheme: light only`
  - `@media (prefers-color-scheme: dark)` で背景・文字・ボタン・枠線を `!important` で明示
  - 変数の再宣言だけに頼らない（第1弾で抜けが発生したため）
- `word-break: auto-phrase` / `line-break: strict` / `text-wrap: balance`
- アニメーションは `transform` と `opacity` のみ、`prefers-reduced-motion` で即時表示
- **AI APIは呼ばない。** 回数制限・体験コード不要、常時公開可
- 公開時のファイル名は `index.html`

---

## 12. スコープ外

- 閲覧者によるデータ投入、パラメータ変更
- 実際の文書照合処理
- 承認フローの経路設定・組織図
- 業種別CONFIG差し替え（1本作り込み方針）
- 第1弾との画面上の相互リンク（両方揃ってから検討）

---

## 13. 未決事項

| # | 項目 | 備考 |
|---|---|---|
| A | ブランド表示と導線 | 第1弾はAXEON。営業主体との整合を確認する |
| B | 承認の丸の数 | 現案は5（受入・加工・中間検査・組立・出荷検査） |
| C | 崩れを1箇所にするか2箇所にするか | 材質記号のずれのみ／ロット番号不足も出す |
| D | S6の一斉描画の速度 | 実機で調整 |
| E | S8の基準更新を本編に入れるか、第2部送りにするか | 本編が9段は長い可能性 |
| F | デモの正式名称 | 未定 |

---

## 14. 受け入れ基準

| # | 基準 |
|---|---|
| G1 | 全テキストを非表示にしても §4 原則1 の3点が読み取れる |
| G2 | 承認の丸が5つとも同じ形・同じ大きさで描かれている |
| G3 | 斜めの線が1本だけである |
| G4 | S2とS6で、同じ線の描画に明確な速度差がある |
| G5 | S6以降も、システムが「不一致」と断定していない（要確認として人に上げている） |
| G6 | S8で基準の改定に承認が付いている |
| G7 | 表示された要素がその後の段で消えていない |
| G8 | 動いている箇所が常に1つ以下 |
| G9 | 改ざん・不正を想起させる語が一切ない |
| G10 | iOSダークモードで文字が読めなくなる箇所がない |
| G11 | 横幅370pxで横スクロールが発生しない |
| G12 | 全段を通して、そのまま声に出せない日本語がない |
